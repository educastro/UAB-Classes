package lab03;

public class MergeSort {
	
	public static void mergeSort(int[] A, int[] temp, p, r){
		if(p < r){
			int middleElement = (p+r)/2;
			mergeSort(A, temp, p, q);
			mergeSort(A, temp, q+1, r);
			merge(A, temp, p, q, r);
		}
	}
	


